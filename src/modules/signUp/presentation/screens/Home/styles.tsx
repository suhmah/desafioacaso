import { Metrics } from '@/acaso_ui';
import styled from 'styled-components/native';

export const Wrapper = styled.View`
  flex: 1;

  width: 100%;

  margin-top: ${Metrics(42)}px;

  padding: 0 ${Metrics(44)}px;
`;
